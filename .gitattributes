# Normaliza finais de linha (evita ruído no git após build no Windows)
* text=auto eol=lf

# Scripts Windows
*.bat text eol=crlf
*.cmd text eol=crlf

# Assets gerados no build
public/seo-static/** text eol=lf
public/sitemap.xml text eol=lf
public/netcar-config.php text eol=lf
