{
  "name": "questionario-perfil-comportamental",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --kv PERFIL_KV --port 8788",
    "deploy": "npm run build && wrangler pages deploy dist --project-name questionario-perfil",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-pages.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.69.0"
  },
  "type": "module"
}
